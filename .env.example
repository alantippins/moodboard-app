# Optional: Default OpenAI API key for development (never commit the actual key)
OPENAI_API_KEY=

# Optional: Rate limiting settings (requests per window)
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes in milliseconds

# Optional: Enable/disable features
ENABLE_VOICE_INPUT=true
ENABLE_SHARING=true
